CREATE TABLE `minions`(
	`id` INT(11) PRIMARY KEY AUTO_INCREMENT,
    `name` VARCHAR(20),
    `age` INT(11)
);

CREATE TABLE `towns`(
	`town_id` INT(11) PRIMARY KEY AUTO_INCREMENT,
    `name` VARCHAR(20)
);
    